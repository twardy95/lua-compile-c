# lua-compile-c
compile c files in lua (Makefile but in lua)

## Example usage:
```lua compile```\
assuming you have lua in your PATH, this should generate a command string and execute it based on the settings inside the file.
